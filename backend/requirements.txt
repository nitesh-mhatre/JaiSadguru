# ---------------------------------------------------------------------------
# Web layer
# ---------------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# ---------------------------------------------------------------------------
# Data
#
# numpy and pandas are declared as floors, not exact pins, on purpose. The backend is
# normally built with conda (see environment.yml), which supplies a tested numpy/pandas
# pair for the interpreter; an exact pin here would make pip try to replace conda's build
# with its own, which is exactly the source-compile path we are avoiding.
#
# Installing these from pip only works on a Python version that has wheels for them —
# 3.10 through 3.13 today. On 3.14+ use the conda environment instead.
# ---------------------------------------------------------------------------
pandas>=2.2.0
numpy>=1.26.0
# Free, keyless market data. Not pinned tightly: yfinance tracks Yahoo's schema
# closely and needs to be updatable without a code change.
yfinance>=0.2.50

# ---------------------------------------------------------------------------
# Kronos model runtime (required by backend/vendor/kronos)
#
# torch publishes aarch64 CPU wheels on PyPI, so this installs cleanly on ARM servers.
# For an x86 machine without a GPU, avoid pulling a multi-gigabyte CUDA build:
#
#   TORCH_INDEX=https://download.pytorch.org/whl/cpu ./setup.sh
# ---------------------------------------------------------------------------
torch>=2.2.0
einops==0.8.1
huggingface_hub==0.33.1
safetensors==0.6.2
tqdm==4.67.1
